# Todo

In progress

## Options
* Version 1.0.3

```
$("element").pan({
	fps: 30,
    speed: 0.7,
    dir: "left",
    depth: 30
});
```

* Version 1.0.4

```
$("element").clouds({
	fps: 30,
    speed: 0.7,
    dir: "left",
    depth: 30
});
```